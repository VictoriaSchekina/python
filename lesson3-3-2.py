numbers = []

def my_func(a, b, c):
    for i in range(my_func):
        i = int(input('Enter a number: '))
         #Вводим 3 числа
        numbers.append(i) #Добавляем их в список
    minimum = min(numbers) #Находим минимальное
    numbers.remove(minimum) #Убираем его из списка
    print(sum(numbers)) #Выдаем сумму